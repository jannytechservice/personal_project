import type { Meta } from '@storybook/react';
import { PhInput } from './PhInput';

const Story: Meta<typeof PhInput> = {
  component: PhInput,
  title: 'composites/PhInput',
};
export default Story;

export const Primary = {
  args: {},
};
