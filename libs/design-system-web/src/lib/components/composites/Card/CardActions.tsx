import React from 'react';
import {
  CardActions as MUICardActions,
  CardActionsProps as MUICardActionsProps,
} from '@mui/material';

export const CardActions = (props: MUICardActionsProps) => (
  <MUICardActions {...props} />
);
