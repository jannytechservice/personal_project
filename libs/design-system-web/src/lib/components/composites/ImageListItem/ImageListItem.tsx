import React from 'react';
import {
  ImageListItem as MUIImageListItem,
  ImageListItemProps as MUIImageListItemProps,
} from '@mui/material';

export const ImageListItem = (props: MUIImageListItemProps) => (
  <MUIImageListItem {...props} />
);
