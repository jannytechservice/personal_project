import React from 'react';
import {
  MobileStepper as MUIMobileStepper,
  MobileStepperProps as MUIMobileStepperProps,
} from '@mui/material';

export const MobileStepper = (props: MUIMobileStepperProps) => (
  <MUIMobileStepper {...props} />
);
