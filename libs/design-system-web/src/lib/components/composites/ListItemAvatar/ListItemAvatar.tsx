import React from 'react';
import {
  ListItemAvatar as MUIListItemAvatar,
  ListItemAvatarProps as MUIListItemAvatarProps,
} from '@mui/material';

export const ListItemAvatar = (props: MUIListItemAvatarProps) => (
  <MUIListItemAvatar {...props} />
);
