import type { Meta } from '@storybook/react';
import { RadioGroup } from './RadioGroup';

const Story: Meta<typeof RadioGroup> = {
  component: RadioGroup,
  title: 'composites/RadioGroup',
};
export default Story;

export const Primary = {
  args: {},
};
