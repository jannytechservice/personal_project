import React from 'react';
import {
  ImageListItemBar as MUIImageListItemBar,
  ImageListItemBarProps as MUIImageListItemBarProps,
} from '@mui/material';

export const ImageListItemBar = (props: MUIImageListItemBarProps) => (
  <MUIImageListItemBar {...props} />
);
